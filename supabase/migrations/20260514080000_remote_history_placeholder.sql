-- Placeholder for a migration version already present in the linked Supabase
-- project's remote migration history. Keeping the version locally prevents
-- Supabase CLI from blocking later migrations because of a remote-only entry.
select 1;
